# is prime

Given
$n \in N$
,
return whether it is prime.

It takes
$O(\log n)$
time complexity.

# prime counting

Given
$n \in N$
,
return
$\sum_{i \in P, i \le n} i$
.
This function is called prime counitng function.

It takes
$O(\sqrt{n})$
time complexity.
